import { createTheme } from '@mui/material'

export const MUI_THEME_CONFIG = createTheme({
  typography: {
    fontFamily: [
      'Be Vietnam Pro',
      'Roboto',
      '"Helvetica Neue"',
      'Arial',
      'sans-serif',
    ].join(','),
  },
})
